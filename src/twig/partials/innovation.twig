{% set innovation_id                    = '2' %}
{% set innovation_background            = 'http://localhost:3030/assets/img/innovation_1.jpg' %}
{% set innovation_title                 = '' %}
{% set innovation_subtitle              = '' %}
{% set innovation_content               = true %}
{% set innovation_content_title         = 'Een bezoek aan jouw toekomstige huis' %}
{% set innovation_content_paragraph     = 'Met de nieuwe VR technologie kun je binnenkort jouw nieuwe huis in lopen zonder dat er iets gebouwd is. Het enige wat je nodig hebt is een Virtual Reality bril. Wanneer je in de VR-wereld zit, kun je door het hele huis lopen. Ook kun je zelf het interieur bepalen en wijzigen. Je kunt jouw huis al compleet aankleden met meubilair en andere spullen, om een levensechte ervaring te krijgen.' %}
{% set innovation_content_button        = true %}
{% set innovation_content_button_text   = 'KPN reclame van VR' %}
{% set button_link                      = 'https://www.youtube.com/watch?v=ZxdLYHF4IbQ' %}
{% set theme                            = '' %}

{% for innovations in range(1, innovation_id) %}
    <section class="innovation innovation-{{ innovations }}" id="anchor-{{ innovations }}">
        <div class="innovation__container">
            <div class="innovation__content">
                {#{% if innovation_id = 1 %}#}
                    {#<h1>hallo</h1>#}
                {#{% endif %}#}
                {% if innovation_title and innovation_subtitle != '' %}
                    <div class="innovation__heading">
                        {% if innovation_title != '' %}
                            <h2 class="innovation__title">{{ innovation_title }}</h2>
                        {% endif %}
                        {% if innovation_subtitle != '' %}
                            <p class="innovation__subtitle">{{ innovation_subtitle }}</p>
                        {% endif %}
                    </div>
                {% endif %}

                {% if innovation_content %}
                    <article class="innovation__wrapper">
                        {% if innovation_content_title != '' %}
                            <h2 class="innovation__title">{{ innovation_content_title }}</h2>
                        {% endif %}
                        {% if innovation_content_paragraph != '' %}
                            <p class="innovation__paragraph">{{ innovation_content_paragraph }}</p>
                        {% endif %}
                        {% if innovation_content_button %}
                            <a class="button button--{{ theme }}" href="{{ button_link }}">
                                <span class="button__text">{{ innovation_content_button_text }}</span>
                            </a>
                        {% endif %}
                    </article>
                {% endif %}
            </div>
        </div>
    </section>
{% endfor %}
